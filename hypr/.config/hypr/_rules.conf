# # ######## Window rules ########
# windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
# windowrule = noblur,.*
# windowrule = float, ^(blueberry.py)$
# windowrule = float, ^(steam)$
# windowrule = float, ^(guifetch)$ # FlafyDev/guifetch
# windowrulev2 = tile,class:(wps)
# windowrulev2 = tile,class:(dev.warp.Warp)
# windowrule = opacity 0.89 override 0.89 override, .* # Applies transparency to EVERY WINDOW
# windowrule=move 100% 0, pseudo,title:^(.*)(is sharing your screen)(.*)$

# Dialogs
windowrule=float,title:^(Open File)(.*)$
windowrule=float,title:^(Select a File)(.*)$
windowrule=float,title:^(Select one or)(.*)$
windowrule=float,title:^(Choose wallpaper)(.*)$
windowrule=float,title:^(Open Folder)(.*)$
windowrule=float,title:^(Save As)(.*)$
windowrule=float,title:^(Library)(.*)$

windowrulev2=float,class:(org.kde.bluedevilwizard)
windowrulev2=center,class:(org.kde.bluedevilwizard)
windowrulev2=size 50% 80%,class:(org.kde.bluedevilwizard)

# windowrule = float, wofi 
# windowrulev2 = stayfocused, class:(wofi)

# ######## Layer rules ########
# layerrule = xray 1, .*
# layerrule = noanim, .*
# layerrule = noanim, selection
# layerrule = noanim, overview
# layerrule = noanim, wofi
# layerrule = noanim, sideleft
# layerrule = noanim, sideright
# layerrule = noanim, osk

# layerrule = noanim, noanim
# layerrule = blur, gtk-layer-shell
# layerrule = ignorezero, gtk-layer-shell
# layerrule = blur, launcher
# layerrule = ignorealpha 0.5, launcher
# layerrule = blur, notifications
# layerrule = ignorealpha 0.69, notifications

